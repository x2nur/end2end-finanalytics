name: 'finanalytics'
version: '1.0.0'
profile: 'finanalytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"


models:
  finanalytics:
    dwh:
      +materialized: table 
      +tags: ["dwh"]

vars:
  interval_start: '2019-10-01'
  interval_end: ''
